# AGP 9.3 automatically includes this file and the default Android rules.
# Gson creates these DTOs through reflection, including nested response types.
-keep,allowoptimization,allowobfuscation class com.truongngo.moviedb.data.network.model.**

# Unannotated fields use their original names as JSON keys. Keep all DTO fields
# for reflective deserialization, while allowing methods to be optimized.
-keepclassmembers,allowoptimization class com.truongngo.moviedb.data.network.model.** {
    <fields>;
}
